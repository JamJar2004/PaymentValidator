<Window x:Class="PaymentValidator.Views.StringValueDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PaymentValidator.Views"
        mc:Ignorable="d"
        Title="Enter Text" WindowStartupLocation="CenterScreen" SizeToContent="Height" Height="auto" Width="400" 
		DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <StackPanel VerticalAlignment="Center">
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="auto"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			
			<TextBlock Grid.Column="0" Margin="5, 5, 5, 2.5" VerticalAlignment="Center" Text="Enter text here:"/>

			<TextBox Grid.Column="1" Margin="2.5, 5, 5, 2.5" FontSize="14" Text="{Binding Text}"/>
		</Grid>
		<Grid Height="40">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<Button x:Name="OkButton" 
					Margin="5, 5, 2.5, 5" 
					Grid.Column="0" 
					Content="OK" 
					Click="OkButton_Click"/>
			
			<Button Margin="2.5, 5, 5, 5" 
					Grid.Column="1" 
					Content="Cancel" 
					IsCancel="True"/>
		</Grid>
	</StackPanel>
</Window>
